# ─────────────────────────────────────────────────────────────────────────────
# CricketStream Overlay — Club Configuration
# Edit this file once. Everything else is automatic.
# Lines starting with # are comments and are ignored.
# ─────────────────────────────────────────────────────────────────────────────

[Club]
# Your club's full name as shown on the scorebar title
name = Your Club CC

# Short abbreviation for the scorebar (max 6 characters)
abbreviation = YOURCC

# Home kit colour in hex format — find yours at htmlcolorcodes.com
home_colour = #1a3a5c

# Your PlayCricket club ID — found in the URL when viewing your club page
# e.g. play-cricket.com/website/results → the number in the URL
playcricket_id =

# Motto shown on replay transition (leave blank to hide)
motto =

[API]
# Your PlayCricket API key — request from ECB/PlayCricket support
# This auto-fills opposition name, umpires, competition on match day
playcricket_key = 

# Anthropic API key — only needed for AI commentary (disabled by default)
# Get a free key at console.anthropic.com
anthropic_key = 

[Scoring]
# Full path to the NV Play / PCS Pro scoreboard output folder
# Windows example: C:/Users/Scorer/Documents/Cricket Matches/_Scoreboards/Output
# Mac example:     /Users/scorer/Documents/Cricket Matches/_Scoreboards/Output
# Leave blank to use PlayCricket widget as fallback (score only, no names)
pcs_output_folder =

# NV Play running on a SEPARATE machine from this server (e.g. its own dedicated Windows
# box, so a VM/scorer laptop isn't sharing the streaming machine's CPU/heat budget). Run
# nvplay_bridge.py on that machine, pointed at its NV Play output folder, then fill in its
# URL and token below — leave pcs_output_folder above blank, this takes over instead.
# Example: http://100.101.102.103:5050  (a Tailscale IP is the recommended way to reach it)
pcs_bridge_url =
pcs_bridge_token =

# Folder containing club badge PNG files (named by PlayCricket club ID)
# Leave blank to use a logos/ folder next to server.py
logos_folder =

# Only fetch fixtures at this ground (partial match, case-insensitive)
# Leave blank to accept any home ground
ground_filter =

[OBS]
# Password set in OBS → Tools → WebSocket Server Settings
obs_password = 

# Folder where OBS saves replay buffer clips
# Windows: C:/Users/YourName/Videos/Replays
# Mac:     /Users/yourname/Movies/Replays
replay_folder =

[Stream]
# YouTube title template — {home} and {away} are replaced automatically
youtube_title = LIVE: {home} vs {away}

# Your YouTube STREAM KEY (YouTube Studio → Go Live → Stream settings). Applied to OBS
# automatically on match day. Recommended over OBS's "connect account" mode, which ENDS
# the whole broadcast if the stream stops (a restart, a quality change, an OBS hiccup) —
# key-based streaming just reconnects. Treat it like a password. Leave blank to manage
# OBS's stream settings yourself.
youtube_stream_key =

# Overs per innings (50 for standard, 40 for T40 etc.)
max_overs = 50

# ── YouTube broadcast control (optional) ──────────────────────────────────────
# Setting the broadcast's title, description, privacy, category and "made for kids"
# flag from the control panel is NOT an API key that goes here — it uses a Google
# OAuth credentials FILE instead. One-time setup:
#   1. Go to https://console.cloud.google.com and create a project
#   2. Enable "YouTube Data API v3" for that project
#   3. Create an OAuth client ID, application type "Desktop app", and download its JSON
#   4. Rename the downloaded file to  yt_credentials.json  and put it next to server.py
#   5. Click "Update YouTube broadcast" in the control panel once — a browser opens to
#      authorise (do this ON THE STREAMING LAPTOP, not a remote device)
# Full walkthrough: https://developers.google.com/youtube/registering_an_application
# and OAuth desktop-app step: https://developers.google.com/youtube/v3/guides/auth/installed-apps
# SECURITY: yt_credentials.json and yt_token.json are secrets — they are git-ignored and
# must never be shared or committed. Anyone with them can post to your channel.

[Auth]
# Cryptographic signing key — used to sign session tokens, never typed by anyone.
# Generate your own: python3 -c "import secrets; print(secrets.token_hex(32))"
# Change this to invalidate all active sessions immediately.
control_token =

# Club password — what operators type in the login form.
# Pick something memorable but hard to guess. Share only with trusted operators.
# Leave blank to disable auth entirely (safe on localhost only).
club_password =

[Network]
# Host address the server listens on.
# 127.0.0.1 — localhost only; safest default. Use this if not using remote access.
# 0.0.0.0   — all interfaces; required for Tailscale or LAN remote access.
#             Phase 1+2 auth (control_token + club_password) must be set before changing this.
bind_host = 127.0.0.1

# Cloudflare Tunnel — gives a public https://*.trycloudflare.com URL, for operators who
# can't install Tailscale. Requires the free 'cloudflared' CLI to be installed separately.
# OFF by default. Only enable once club_password (above) is set — with this on, anyone
# with the URL can reach the login page, so the password is the only thing gating them.
cloudflare_tunnel = false
